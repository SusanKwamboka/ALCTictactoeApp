<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kwamboka.tic_tac_toe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Intro2" />
        <activity android:name=".Table" />
        <activity android:name=".Five" />
        <activity android:name=".Twoplayer3" />
        <activity android:name=".Twoplayer5" />
        <activity android:name=".Twoplayermenu" />
        <activity android:name=".Mainmenu" />
    </application>

</manifest>